name: Test

on:
  workflow_dispatch:
  
jobs:
  # This workflow contains a single job called "build"
  deploy:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
      # using fetch-depth = 0 to make the entire history of commits available
      
    - name: Caching the whole sfdx-cli
      uses: actions/cache@v3
      with:
        path:  ~/.npm
        key: test-build-${{ env.cache-name }}-${{github.ref_name}}

    - uses: actions/checkout@v2
      with: 
        fetch-depth: 0

    - name: Caching the whole sfdx-cli
      id: cachehit
      uses: actions/cache@v3
      with:
        path:  ~/.npm
        key: test-build-${{ env.cache-name }}-${{github.ref_name}}

    - name: Setup Node
      uses: actions/setup-node@v1
      with: 
        node-version: '16'

    # Install SFDX CLI echo "GH_TOKEN=${{ secrets.token }}" >> $GITHUB_ENV
    - name: Installations and Support Tasks
      if: steps.cache.outputs.cachehit != true
      run: npm install sfdx-cli --global
    
    - name: installing sgd
      run: echo y | sfdx plugins:install sfdx-git-delta

#     - name: Caching the whole sfdx-cli
#       uses: actions/cache@v3
#       with:
#         path:  ~/.npm
#         key: test-build-${{ env.cache-name }}-${{github.ref_name}}

        
        
    

#     steps:      
#     - run: echo "Jay is great" >> chamberlain.txt
        
#     - name: Storing Artifacts
#       uses: actions/download-artifact@v2
#       with: 
#         name: Jay test
#         path: ./chamberlain.txt

